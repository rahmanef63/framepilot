/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.DS_Store
npm-debug.log*
.env*.local

# Added by si-coder deploy (secret-leak guard)
.env
.env.*
!.env.example

# Playwright
/test-results
/playwright-report

# Convex snapshot exports — contain user data (emails); never commit
/backups
*-convex-*.zip

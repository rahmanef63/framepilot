import { StubScreen } from "@/components/StubScreen";

export default function BerandaPage() {
  return (
    <StubScreen
      title="Beranda · Home"
      desc="Ringkasan proyek & aktivitas terbaru. Layar contoh dalam shell platform — demo lengkap ada di Data Prompt. · Sample screen in the platform shell."
    />
  );
}

import { StubScreen } from "@/components/StubScreen";

export default function TemplatePage() {
  return (
    <StubScreen
      title="Template · Templates"
      desc="Template shot & angle siap pakai. Layar contoh dalam shell platform — demo lengkap ada di Data Prompt. · Sample screen in the platform shell."
      cards={4}
      cardLabel="Template"
    />
  );
}
